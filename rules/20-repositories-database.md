# 20 — Repositories & Database

- Persistence only: typed queries, no business/AI/file-security decisions.
- Pagination for any list; indexes documented next to the query.
- Twinzy currently persists nothing by design (no images, no results); the layer exists for
  future non-sensitive data only. Never add image or biometric persistence.
