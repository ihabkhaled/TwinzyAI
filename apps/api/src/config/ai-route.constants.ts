/** Maximum provider/model entries accepted in one configured AI route. */
export const MAX_AI_ROUTE_ENTRIES = 10;

/** Maximum provider/model calls one pipeline step may attempt before failing. */
export const MAX_AI_MODEL_ATTEMPTS = 3;
