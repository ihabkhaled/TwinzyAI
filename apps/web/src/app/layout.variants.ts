/**
 * Style recipes for the root document shell. Kept in a `*.variants.ts` file so
 * the route layer stays free of raw className strings (the design-system rule).
 */
export const bodyClassName = 'bg-background text-foreground antialiased';
