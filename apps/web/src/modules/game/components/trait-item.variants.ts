/** Class bundles for a single extracted-trait row. */
export const traitItemClass = 'flex flex-col gap-0.5 rounded-lg bg-muted px-3 py-2';

export const traitItemLabelClass =
  'text-xs font-semibold uppercase tracking-wide text-muted-foreground';

export const traitItemValueClass = 'text-sm text-foreground';
