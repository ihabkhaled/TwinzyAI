/** Class bundles for the upload/camera source card. */
export const uploadGridClass = 'grid grid-cols-1 gap-3 sm:grid-cols-2';

export const uploadSourceButtonClass =
  'flex min-h-32 cursor-pointer flex-col items-center justify-center gap-2 rounded-xl border-2 border-dashed border-border p-4 text-center hover:bg-muted focus-within:ring-2 focus-within:ring-primary';

export const uploadSourceTitleClass = 'text-base font-semibold text-foreground';

export const uploadSourceHintClass = 'text-sm text-muted-foreground';

export const uploadSrOnlyClass = 'sr-only';

export const uploadPreviewWrapClass = 'mt-4 flex justify-center';

export const uploadPreviewImageClass = 'max-h-60 w-auto rounded-xl object-contain';

export const uploadErrorClass = 'mt-3';
