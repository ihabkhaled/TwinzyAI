/** Inputs for {@link buildSocialMetadata}: the copy a link preview shows. */
export interface SocialMetadataInput {
  title: string;
  description: string;
  siteName: string;
  ogLocale: string;
  path: string;
}
