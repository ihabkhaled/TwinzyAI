# 04 — Cross-Functional Refinement

Product owns honest copy; frontend owns routing/rendering; security owns canonical-host, XML, contact
input, CSP, and private-route boundaries; i18n owns dictionary parity and RTL; release owns Vercel,
hooks, CI, Docker, rollback, and immediate push evidence.
