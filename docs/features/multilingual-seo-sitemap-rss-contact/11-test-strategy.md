# 11 — Test Strategy

Unit tests cover locale parsing/replacement, query preservation, route classification, canonical and
hreflang generation, sitemap uniqueness/exclusions, XML escaping/control stripping/bounds, contact
validation and URL encoding, robots behavior, and dictionary parity. Playwright covers representative
LTR/RTL locale routes, switching, legacy redirect, contact keyboard flow, XML responses, and noindex.
