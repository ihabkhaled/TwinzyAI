# Cross-Functional Refinement

- Product: enhanced matching must be restored, not removed.
- Reliability: a provider-format deviation may not burn all models when it can be mapped without
  inventing high-confidence evidence.
- AI safety/privacy: normalization logs counts/paths only; no model values, prompt, or image.
- Payments: local reproduction uses free-by-default configuration; transactional delivery behavior
  remains unchanged and is regression-tested.
- QA: exercise the real multipart/SSE boundary with a deterministic provider double and inspect
  exact step calls.
- Operations: correlate post-deploy logs for extraction served, then generation and judge.

