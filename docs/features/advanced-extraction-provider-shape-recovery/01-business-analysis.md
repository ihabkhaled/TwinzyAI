# Business Analysis

Players can approve a payment and wait through three provider attempts, but receive no game result
because the extraction response never crosses the strict schema boundary. This damages completion,
cost, and trust.

Success means one valid upload reaches extraction, generation, judging, aggregation, and terminal
delivery; enhanced evidence remains present; a failed paid run still cannot retain captured money.
The attached chat image is not available as a workspace file, so the automated backend trial uses
the repository's decoded JPEG fixture while the production trace supplies the real-image evidence.

