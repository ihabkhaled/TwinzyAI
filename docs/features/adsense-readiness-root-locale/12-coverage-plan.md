# 12 — Coverage Plan

Every touched executable module must meet repository per-file thresholds. New route branches cover root,
machine endpoints, localized pages, unsupported paths, changed/unchanged/invalid locale selection, and
configured/unconfigured AdSense behavior. Configuration and static content receive build/curl verification.
