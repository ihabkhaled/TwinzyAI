# 11 — Test Strategy

Unit tests cover pure route/alternate rules, proxy responses, hook transition state, component busy UI,
and AdSense rendering. Build output and Playwright cover integration. Curl validates live status,
redirects, content type, final URL, exact text, metadata, sitemap, robots, and script count. Full lint,
typecheck, unit, coverage, integration, E2E, build, security, and Knowledge gates apply.
