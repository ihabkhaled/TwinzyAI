# 06 — Technical Refinement

Reuse `proxy.ts`, locale-route helpers, the root metadata owner, the existing AdSense package, the
locale-switch hook/container, shared Spinner, and existing content-navigation helpers. No dependency,
new abstraction, backend, schema, persistence, API, or infrastructure service is required.
