# Coverage Plan

Touched shared schemas, matching-profile utilities, catalog/domain/services/adapters/cache, ensemble/critique/scoring services, config, game orchestration, aggregation, language guard, result/share view models, modal hooks/components, and benchmark files require per-file coverage of 95% statements, 90% branches, 95% functions, and 95% lines.

Critical scenario coverage is mandatory for extraction-only image flow, text-only ensemble/enrichment, deterministic median scoring, adapter allowlisting, bounded cache/second pass, Arabic rejection/translation, safe optional fallback, and accessible modal focus lifecycle.

No waiver is requested. Coverage evidence will be recorded in phase 15 after `npm run test:coverage`.

