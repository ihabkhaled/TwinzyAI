# Reliability Patterns

- finally-based cleanup for the image buffer (success and failure paths).
- Fail-closed virus scanning in production.
- Safe error envelope + stable error codes; frontend maps codes to friendly i18n messages.
- Health endpoints wired into Docker healthchecks.
